rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Logos: el admin de esa empresa puede subir, cualquiera puede leer
    match /logos/{userId} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
